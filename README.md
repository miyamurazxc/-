Веб-приложение для хранения, сортировки и поиска контактных данных пользователей.

Проект реализован на Java с использованием Spring Boot и демонстрирует работу с многослойной архитектурой, базой данных и системой аутентификации.

🔹 Основной функционал

Регистрация и авторизация пользователей

Добавление, редактирование и удаление контактов

Поиск по имени, email, телефону и другим полям

Сортировка и фильтрация списка контактов

Пагинация данных

Загрузка изображений для контактов

Защита данных с помощью Spring Security и BCrypt

🔹 Используемые технологии

Java

Spring Boot, Spring MVC

Spring Data JPA, Hibernate

Spring Security

MySQL

Thymeleaf

Bootstrap

Maven

🔹 Назначение проекта

Учебный проект, демонстрирующий принципы объектно-ориентированного программирования, работу с базой данных и разработку серверных веб-приложений на Java.# -
## Технологический стек

- **Java 11**
- **Spring Boot** — запуск приложения и автоконфигурация
- **Spring MVC** — обработка HTTP-запросов (контроллеры)
- **Spring Data JPA + Hibernate** — работа с БД через ORM
- **Spring Security** — аутентификация и авторизация
- **MySQL** — хранение пользователей и контактов
- **Thymeleaf** — серверная генерация HTML
- **Bootstrap** — оформление интерфейса
- **Maven** — сборка и зависимости

---

## Структура базы данных

### Таблица `USER`
- `id` — идентификатор
- `name` — имя пользователя
- `email` — логин (уникальный)
- `password` — BCrypt-хеш пароля
- `role` — роль (например, `ROLE_USER`)
- `enabled` — активен ли пользователь
- `imageUrl` — аватар
- `about` — описание

### Таблица `CONTACT`
- `cId` — идентификатор контакта
- `name` — имя
- `secondName` — ник/фамилия
- `phone` — телефон
- `email` — email
- `work` — работа/должность
- `description` — заметки
- `image` — имя файла фото
- `createdAt` — дата добавления
- `user_id` — внешний ключ на `USER`

- ## Структура проекта
- contactmanager/
├─ src/                                           ← исходники проекта
│  ├─ main/                                       ← основной код приложения
│  │  ├─ java/com/smart/                           ← Java-код (backend)
│  │  │  ├─ controller/                            ← контроллеры Spring MVC (обработка запросов)
│  │  │  │  ├─ HomeController.java                 ← страницы: главная, about, регистрация, логин
│  │  │  │  └─ UserController.java                 ← личный кабинет: CRUD контактов, поиск, сортировка, фильтры
│  │  │  ├─ config/                                ← настройка Spring Security
│  │  │  │  ├─ MyConfig.java                       ← правила доступа, страница логина, обработка входа, роли
│  │  │  │  ├─ UserDetailsServiceImpl.java         ← загрузка пользователя из БД по email для Security
│  │  │  │  └─ CustomUserDetails.java              ← адаптер User → UserDetails (пароль/логин/роль)
│  │  │  ├─ dao/                                   ← репозитории (доступ к БД через Spring Data JPA)
│  │  │  │  ├─ UserRepository.java                 ← запрос пользователя по email (логин)
│  │  │  │  └─ ContactRepository.java              ← контакты: список, поиск, фильтры, пагинация
│  │  │  ├─ entities/                              ← сущности JPA (таблицы базы данных)
│  │  │  │  ├─ User.java                           ← таблица USER: данные аккаунта + список контактов
│  │  │  │  └─ Contact.java                        ← таблица CONTACT: контакт + ссылка на владельца (User)
│  │  │  └─ helper/                                ← вспомогательные классы
│  │  │     └─ Message.java                        ← сообщение в сессии (текст + тип успех/ошибка)
│  │  └─ resources/                                ← ресурсы приложения (шаблоны, статика, конфиг)
│  │     ├─ templates/                             ← Thymeleaf-страницы (HTML шаблоны)
│  │     │  ├─ base.html                           ← общий шаблон для публичных страниц (navbar + контент)
│  │     │  ├─ home.html                           ← главная страница (баннер/кнопка “начать”)
│  │     │  ├─ login.html                          ← форма входа (Spring Security loginPage)
│  │     │  ├─ signup.html                         ← форма регистрации (создание USER)
│  │     │  └─ normal/                             ← страницы после входа (личный кабинет)
│  │     │     ├─ user_dashboard.html              ← главная страница кабинета
│  │     │     ├─ add_contact_form.html            ← форма добавления контакта (Create)
│  │     │     ├─ show_contacts.html               ← таблица контактов + поиск/сортировка/пагинация (Read)
│  │     │     ├─ update_contact.html              ← форма редактирования контакта (Update)
│  │     │     ├─ profile.html                     ← профиль пользователя (данные USER)
│  │     │     └─ settings.html                    ← настройки (страница настроек)
│  │     ├─ static/                                ← статические файлы (CSS/JS/картинки)
│  │     │  ├─ css/style.css                       ← стили: цвета, banner, sidebar, оформление страниц
│  │     │  ├─ js/script.js                        ← JS: открытие/закрытие sidebar и мелкая логика
│  │     │  └─ img/                                ← изображения интерфейса и загруженные фото контактов
│  │     │     ├─ banner.jpg                       ← фон баннера на главной
│  │     │     ├─ membership.png                   ← иконка на странице регистрации
│  │     │     └─ (загруженные изображения контактов) ← фото, которые сохраняются при добавлении контакта
│  │     └─ application.properties                 ← настройки: подключение к БД, JPA, порт и т.д.
├─ pom.xml                                        ← Maven: зависимости (Spring Boot, JPA, Security, MySQL)
└─ README.md                                      ← описание проекта, запуск, структура, возможности

